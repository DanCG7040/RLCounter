# Analizador de replays de Rocket League
# Python 3.11 (64-bit). Entorno virtual recomendado.
#
# Crear venv:  py -3.11 -m venv .venv
# Activar:     .venv\Scripts\Activate.ps1  (PowerShell)
# Instalar:    pip install -r requirements.txt
#
# boxcars-py requiere Rust (rustup default stable) y en Windows
# "Build Tools para Visual Studio" (MSVC). Ver SETUP_WINDOWS.md.

sprocket-boxcars-py>=0.3.26
